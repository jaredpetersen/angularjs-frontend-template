'use strict';

angular.module('tangram').controller('MainController', function($scope) {
    // create a message to display in our view
    $scope.message = 'This is the main page. Controlled by MainController.';
});
