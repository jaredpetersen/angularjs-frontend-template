'use strict';

angular.module('tangram').controller('AboutController', function($scope) {
    // create a message to display in our view
    $scope.message = 'This is the about page. Controlled by the AboutController';
});
