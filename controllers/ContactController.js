'use strict';

angular.module('angularjs-frontend').controller('ContactController', function($scope) {
    // create a message to display in our view
    $scope.message = 'This is the contact page. Controlled by the ContactController.';
});
